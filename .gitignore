# Gradle, from B-01 onwards.
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
.kotlin/

# Kotlin/Native and IDE.
.konan/
.idea/
*.iml

# Measurements are written by `:server:measure --stand` into docs/research/ and are committed;
# a local run writes nothing, so there is nothing to ignore here. See B-13.

.DS_Store
keel.db
